#ifndef _STRINGS_INCLUDE
#define _STRINGS_INCLUDE
/* --- */


char* parseMessageTag(char *, char *);
char* substring(char*, int, int);
void removeChar(char str[], char ch);
//char* replaceChar(char*, char ch, char to);

#endif

